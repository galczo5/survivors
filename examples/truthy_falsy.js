function printInfo(numberOfDays) {
    if (!numberOfDays) {
        console.log('Release is today!');
    }
}

// 1
const daysToRelease1 = 0;
printInfo(daysToRelease1);

// 2
const daysToRelease2 = new Number(0);
printInfo(daysToRelease2);
